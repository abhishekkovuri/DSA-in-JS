/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
var strStr = function(h, n) {
    return h.indexOf(n);
    // let l = 0;
    // let r = 0;

    // while(l !== h.length && r !== n.length) {
    //     if(n[r] === h[l]) {
    //         r++
    //         l++
    //     } else {

    //     }
    // }
    // return -1;


};